import UIKit

// SceneDelegate is declared here to satisfy the UISceneDelegateClassName reference
// in Info.plist but is not used — FlutterAppDelegate handles all lifecycle events.
class SceneDelegate: UIResponder, UIWindowSceneDelegate {
  var window: UIWindow?
}
